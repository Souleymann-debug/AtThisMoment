{% extends 'base.html.twig' %}


{% block title %} Les commentaires{% endblock %}
  
{% block body %}

<div class="container">

{{ form_start(form, {'attr': {'novalidate': 'novalidate'}})}}
{{ form_row(form.contenu)}}
{{ form_row(form.dateComment)}}


<button type="submit" class="btn btn-primary" >Envoyer</button>
	{{form_end(form)}}
</div>

    <div class="row">
    {# <form method="post" class="col" action="{{ path('sup-commentaire', {'id': commentaire.id}) }}"> #}
    <form method="post" action="{{ path('sup-commentaire', {'id': commentaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
         {# <input type="hidden" name="_method" value="SUP"> #}
        <input type="hidden" name="_token" value="{{ csrf_token('SUP' ~ commentaire.id) }}">
        <input type="submit" class="btn btn-danger w-200" value="supprimer">
    </form>
    </div>




{% endblock %}
